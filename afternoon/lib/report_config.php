<?php
declare(strict_types=1);

// PROTOTYPE / UNCALIBRATED — เกณฑ์เตือน "อาจมีรายการแจ้งซ้ำ" สำหรับ Demo เท่านั้น
// ห้ามอ้างว่าค่าพวกนี้ผ่านการ validate กับพื้นที่บางแสนหรือกับพฤติกรรมผู้ใช้จริงแล้ว
//
// ระยะ 150 เมตรเลือกมาแบบยังไม่ calibrate: geolocation ของมือถือทั่วไปมี error
// ได้ราว 10-50 เมตร (และแย่กว่านั้นถ้าอยู่ในอาคาร/ใช้ Wi-Fi positioning)
// ถ้าตั้งแคบเท่ากับ error ของอุปกรณ์ จุดเดียวกันจะกลายเป็นคนละจุดบ่อยเกินไป
// ค่าที่เหมาะจริงต้องวัดจากข้อมูลการแจ้งซ้ำในพื้นที่ก่อน
const REPORT_DUPLICATE_RADIUS_METERS = 150.0;

// ดูย้อนหลังแค่ช่วงสั้น ๆ เพราะรายการเก่ามากไม่ใช่ "ซ้ำ" อีกแล้ว
const REPORT_DUPLICATE_WINDOW_DAYS = 7;

// เตือนอย่างมากกี่รายการ — เป็นคำเตือน ไม่ใช่ผลค้นหา
const REPORT_DUPLICATE_MAX_RESULTS = 5;
