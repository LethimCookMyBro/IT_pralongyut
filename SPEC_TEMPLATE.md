# SPEC — Bangsaen Waste Vision

> เอกสารนี้เป็นข้อกำหนดหลักของเว็บสำหรับ Workshop  
> เน้นว่า **เว็บต้องทำอะไร**, **ใช้ข้อมูลอะไร**, และ **ต้องพิสูจน์อะไรได้จริง**  
> รายละเอียดธุรกิจให้อ้างอิง `BMC.md` และรายละเอียดสถานะงานให้อ้างอิง `docs/BANGSAEN_WASTE_VISION_PLAN.md`

## 1. ชื่อและเป้าหมาย

**ชื่อเว็บ:** Bangsaen Waste Vision

**ปัญหาที่ต้องการช่วยแก้:**  
พื้นที่สาธารณะและทางน้ำบางจุดอาจมีขยะสะสม แต่เจ้าหน้าที่ไม่สามารถเฝ้าดูทุกจุดหรือทุกกล้องได้ตลอดเวลา ระบบจึงช่วยคัดกรองจุดที่ควรตรวจสอบ และให้เจ้าหน้าที่เป็นผู้ยืนยันก่อนดำเนินการ

**ผู้ใช้หลัก**
- เจ้าหน้าที่เทศบาล / เจ้าหน้าที่สิ่งแวดล้อม
- ทีมดูแลความสะอาด ชายหาด และพื้นที่สาธารณะ

**ผู้ใช้รอง**
- ประชาชน ร้านค้า และนักท่องเที่ยวที่ใช้หน้าแจ้งจุดขยะ

---

## 2. ฟีเจอร์หลักที่ต้องมี

### 2.1 หน้าสถิติขยะ
ใช้ข้อมูลเปิดของจังหวัดชลบุรีที่นำเข้าไว้ในฐานข้อมูล

ต้องแสดงอย่างน้อย:
- ปริมาณขยะที่เกิดขึ้น
- ปริมาณที่เก็บขน
- ปริมาณที่นำไปใช้ประโยชน์
- ปริมาณกำจัดถูกต้อง / ไม่ถูกต้อง
- อัตราการกำจัดถูกต้อง
- 5 อปท. ที่มีปริมาณขยะเกิดขึ้นสูงสุด

ข้อมูลส่วนนี้เป็น **ข้อมูลย้อนหลัง** ไม่ใช่ข้อมูลสดจากกล้อง และไม่ใช้เป็นข้อมูลฝึก Computer Vision

### 2.2 หน้าแจ้งจุดขยะ

หน้านี้มีผู้ใช้เป็น **ประชาชนทั่วไป** ไม่ใช่เจ้าหน้าที่ที่คุ้นกับแบบฟอร์มราชการ
ต้นทุนในการแจ้งหนึ่งเรื่องต้องต่ำที่สุดเท่าที่ยังได้ข้อมูลที่ใช้งานได้

**ข้อมูลขั้นต่ำของหนึ่งเรื่องแจ้ง:**

| ฟิลด์ | บังคับ? | หมายเหตุ |
|---|---|---|
| สถานที่ (`location`) | **บังคับ** | ถ้าไม่รู้ว่าที่ไหน เรื่องแจ้งใช้งานไม่ได้ |
| รูป (`image_path`) | ไม่บังคับ | ถ่ายจากมือถือหรือเลือกไฟล์ |
| รายละเอียด (`detail`) | ไม่บังคับ | ช่องเดียว ไม่มีช่องย่อย |

**ฟิลด์ที่ไม่อยู่ในฟอร์มของประชาชนแล้ว:**
- `waste_type` — ประชาชนไม่ควรต้องจำแนกประเภทขยะให้ถูกก่อนแจ้งได้
- `amount_kg` — คนทั่วไปประเมินน้ำหนักเป็นกิโลกรัมจากการมองไม่ได้

สองฟิลด์นี้ยังอยู่ใน schema เป็น **NULL ได้** เพราะ:
- เรื่องแจ้งเก่าที่มีค่าอยู่แล้วต้องไม่เสียข้อมูล
- เจ้าหน้าที่หรือขั้นตอนถัดไปอาจเติมภายหลัง

**ห้ามเติมค่าปลอมหลังบ้านเพื่อให้ validation ผ่าน**
ไม่มีค่า = `NULL` และหน้ารายการแสดง `—` ไม่ใช่เดาค่าแทนผู้ใช้

ระบบต้อง:
- ตรวจข้อมูลฝั่ง server (ห้ามเชื่อ client)
- ตรวจไฟล์รูปฝั่ง server: ชนิดไฟล์จริง ขนาด และ **สร้างชื่อไฟล์เอง**
  ห้ามใช้ชื่อไฟล์จากผู้ใช้ และห้ามให้ path หลุดออกนอกโฟลเดอร์อัปโหลด
- เก็บเฉพาะ path ของรูปลง MySQL ไม่เก็บ binary/base64
- บันทึกลง MySQL
- แสดงรายการแจ้งล่าสุดและสถานะ

#### 2.2.1 AI ช่วยลดการกรอก — ขอบเขตที่อนุญาต

เป้าหมายระยะถัดไปคือให้โมเดลช่วยเติมสิ่งที่ประชาชนไม่ควรต้องกรอกเอง

AI **อาจ** เสนอได้ (เมื่อมี inference จริง):
- ประเภทขยะที่มองเห็นในรูป
- จำนวนวัตถุที่ตรวจพบ
- ระดับปริมาณแบบหยาบ: น้อย / ปานกลาง / มาก
- ค่า confidence ของโมเดล

AI **ห้าม** อ้าง:
- น้ำหนักเป็นกิโลกรัมจากรูปเดียว — รูปทั่วไปไม่มี scale / depth / density
  พอที่จะอนุมานมวลได้ จนกว่าจะมีวิธีวัดที่ผ่านการตรวจสอบแล้ว
- ผลที่ไม่ได้มาจาก inference จริง — **ห้ามสร้างผล AI ปลอมทุกกรณี**

ข้อบังคับเชิงสถาปัตยกรรม:
- การส่งเรื่องแจ้ง **ต้องไม่ขึ้นกับ AI** — ไม่มีโมเดล ไม่มี worker ก็ต้องส่งเรื่องได้
- ถ้ายังไม่มีช่องทางนำรูปของประชาชนเข้าโมเดลจริง ให้ทำแค่ upload + preview
  แล้วปล่อยฟิลด์ AI เป็น NULL ตามความจริง

### 2.3 Waste Vision
เป็นฟีเจอร์เด่นของกลุ่ม

Prototype วันนี้ใช้ **REPLAY MODE** เพราะยังไม่ได้เชื่อม CCTV เทศบาลจริง

แนวคิดหลัก:

```text
ภาพ / วิดีโอทดสอบ
→ Local Computer Vision
→ เก็บผลการตรวจพบแต่ละครั้ง
→ รวมผลที่เกิดซ้ำจากจุดเดียวกันเป็นเหตุเดียว
→ แสดงคิวเหตุที่ควรตรวจ
→ เจ้าหน้าที่ Confirm / Reject
→ ถ้า Confirm = Needs Check
→ เมื่อดำเนินการแล้ว = Resolved
```

หลักสำคัญ:

**การตรวจพบ 1 ครั้ง ไม่เท่ากับ 1 เหตุ และไม่เท่ากับ 1 งาน**

ระบบต้องไม่สร้างงานใหม่ทุกครั้งที่กล้องเห็นขยะซ้ำ

---

## 3. ข้อมูลที่ใช้

### ข้อมูลสถิติ
ไฟล์:
`data/waste_chonburi.csv`

ที่มา:
ข้อมูลเปิดของสำนักงานทรัพยากรธรรมชาติและสิ่งแวดล้อมจังหวัดชลบุรี  
https://chonburi.gdcatalog.go.th/dataset/?organization=chonburi_mnre

ข้อมูลถูกนำเข้า MySQL เพื่อให้เว็บอ่านใช้งาน ไม่ได้ดึงสดจากเว็บไซต์ต้นทางทุกครั้ง

### ข้อมูลแจ้งขยะ
มาจากฟอร์มบนเว็บและบันทึกลง MySQL

### ข้อมูล Waste Vision
มาจากผล inference ของ Local Computer Vision

Prototype ใช้ภาพอ้างอิงสำหรับทดสอบระบบ  
**ยังไม่ใช่ภาพจาก CCTV เทศบาลหรือชุดข้อมูลบางแสนจริง**

---

## 4. ฐานข้อมูล

ตารางหลัก:

- `waste_stats` — ข้อมูลสถิติขยะ
- `reports` — รายการแจ้งจุดขยะ
- `vision_observations` — ผลที่ AI ตรวจพบแต่ละครั้ง
- `vision_incidents` — เหตุที่รวมผลตรวจซ้ำแล้ว เพื่อให้เจ้าหน้าที่ตรวจ

หลักการ:
- คนตรวจและเปลี่ยนสถานะที่ระดับ `vision_incidents`
- `vision_observations` ใช้เป็นข้อมูลประกอบของเหตุ
- confidence เป็นข้อมูลช่วยดูเท่านั้น ไม่ใช่หลักฐานว่าขยะได้รับการยืนยันแล้ว
- `reports.waste_type` และ `reports.amount_kg` เป็น **NULL ได้** (ดู 2.2)
  แถวที่ค่าเป็น NULL คือ "ไม่รู้" ไม่ใช่ "ศูนย์"
- `reports.image_path` เก็บ path ของรูปที่ผู้ใช้แนบ (NULL ได้)
  ไฟล์จริงอยู่ใน `afternoon/uploads/reports/` ชื่อไฟล์ระบบเป็นคนตั้ง

รายละเอียดคอลัมน์ให้อ้างอิง:
`afternoon/sql/schema.sql`

---

## 5. API หลัก

### สถิติ
- `GET /api/stats.php`

### แจ้งจุดขยะ
- `GET /api/reports.php`
- `POST /api/reports.php` — รับได้สองแบบ:
  - `application/json` (ไม่มีรูป)
  - `multipart/form-data` (มีรูป, field ชื่อ `photo`)

### Waste Vision
- `GET /api/vision.php` — อ่านรายการเหตุและสรุปสถานะ
- `POST /api/vision.php` — รับผลตรวจจาก Local Computer Vision
- `GET /api/vision-observations.php?incident_id=...` — ดูผลตรวจที่อยู่ในเหตุหนึ่ง
- `POST /api/vision-review.php` — Confirm / Reject / Resolve เหตุ
- `GET /api/live-detection.php` — อ่านสถานะและผลล่าสุดของ Local detector worker โดยไม่แตะฐานข้อมูล

---

## 6. กฎสำคัญของ Waste Vision

สำหรับ Prototype:
- ผลตรวจที่มาจากจุดเดียวกันและเกิดใกล้กัน สามารถรวมเป็นเหตุเดียวได้
- ค่าช่วงเวลารวมเหตุอยู่ใน `afternoon/lib/vision_config.php`
- กฎนี้ยังเป็น **PROTOTYPE / UNCALIBRATED**
- AI ห้ามยืนยันเหตุแทนคน
- AI ห้ามสร้างคำสั่งเก็บขยะอัตโนมัติ
- เจ้าหน้าที่ต้องเป็นผู้ Confirm หรือ Reject

---

## 7. สิ่งที่ต้อง Demo ได้

ต้องพิสูจน์ได้ว่า:

1. หน้าสถิติอ่านข้อมูลจาก MySQL ได้จริง
2. หน้าแจ้งขยะส่งข้อมูลเข้า MySQL ได้จริง โดยกรอกแค่ **สถานที่** อย่างเดียวก็ส่งได้
2ก. แนบรูปแล้วไฟล์ถูกบันทึกด้วยชื่อที่ระบบตั้งเอง และ DB เก็บแค่ path
3. Local Computer Vision รันกับภาพจริงได้
4. ผลตรวจส่งเข้า PHP + MySQL ได้
5. การตรวจซ้ำจากจุดเดียวกันสามารถรวมเป็นเหตุเดียวได้
6. Dashboard แสดงเหตุที่รอตรวจได้
7. เจ้าหน้าที่ Confirm / Reject ได้
8. เหตุที่ Confirm แล้วสามารถเปลี่ยนเป็น Resolved ได้
9. หน้า `detect.html` เล่น **วิดีโอที่ผ่านการ annotate ด้วย inference จริง** ได้
   และกดสลับ ริมถนน / พื้นที่สาธารณะ / ทางน้ำ แล้ววิดีโอเปลี่ยนจริง
10. เมื่อไม่มีไฟล์วิดีโอหรือไม่มี worker (เช่น Railway) หน้า `detect.html` ต้องยังเปิดได้
    แสดงเฟรมตัวอย่างที่ commit ไว้ และบอกตามจริงว่าโหมดวิดีโอทำงานบนเครื่อง local
    — ต้องไม่ดูเหมือนหน้า error
11. หน้าเว็บระบุโหมดเดโมด้วย badge ขนาดเล็ก ส่วนข้อจำกัดการปรับเทียบและรายละเอียดทางเทคนิคอยู่ในส่วนพับได้

---

## 8. สิ่งที่ห้ามพูดเกินหลักฐาน

ห้ามอ้างว่า:
- เชื่อม CCTV เทศบาลจริงแล้ว
- โมเดลมีความแม่นยำกับบางแสนตามตัวเลขใด ๆ โดยยังไม่ได้ทดสอบ
- ตรวจขยะได้ทุกกล้องหรือทุกสภาพ
- กฎรวมเหตุผ่านการปรับเทียบกับพื้นที่จริงแล้ว
- AI เป็นผู้ยืนยันเหตุหรือสั่งงานแทนเจ้าหน้าที่
- ระบบลดเวลา ค่าใช้จ่าย หรือแรงงานได้กี่เปอร์เซ็นต์โดยยังไม่ได้ทดลองจริง
- AI บอกน้ำหนักขยะเป็นกิโลกรัมจากรูปถ่ายธรรมดา (ดู 2.2.1)
- มีผลวิเคราะห์รูปของประชาชนด้วย AI แล้ว ทั้งที่ยังไม่ได้ต่อ inference จริงกับรูปนั้น

---

## 9. Definition of Done สำหรับ Workshop

ถือว่าผ่านเมื่อ:
- Python tests ผ่าน
- PHP validation tests ผ่าน
- Stats ใช้ข้อมูลจริงจากฐานข้อมูล
- Report บันทึกและอ่านข้อมูลได้ โดย**กรอกแค่สถานที่ก็ส่งได้**
- แนบรูปได้จริง ผ่านการตรวจฝั่ง server และ preview/ลบ/เปลี่ยนรูปก่อนส่งได้
- `waste_type` / `amount_kg` ที่ไม่มีค่าแสดงเป็น `—` ไม่ใช่ค่าที่เดาขึ้นมา
- Waste Vision รัน detector จริงอย่างน้อย 1 แหล่งภาพ/วิดีโอ
- `detect.html` ใช้วิดีโอที่ annotate จาก inference จริงเป็นภาพหลักของหน้า
  และสลับแหล่งวิดีโอได้จริง
- Local worker เขียนผลล่าสุดแบบ atomic และหน้า `detect.html` อ่านได้
- ผล detector เข้า MySQL ได้ด้วย `record_origin = detector_run`
- ระบบรวมการตรวจซ้ำเป็น Incident ได้
- Human Review ทำงานที่ระดับ Incident
- Demo ทำงานผ่าน localhost
- Railway web/API หลักเปิดได้ โดย detector แสดง offline ตามจริงเมื่อไม่มี Local worker
- ไม่มีข้อความหรือ claim เกินสิ่งที่พิสูจน์แล้ว
