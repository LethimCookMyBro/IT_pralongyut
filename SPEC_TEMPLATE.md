# SPEC — Bangsaen Waste Vision

> เอกสารนี้เป็นข้อกำหนดหลักของเว็บสำหรับ Workshop  
> เน้นว่า **เว็บต้องทำอะไร**, **ใช้ข้อมูลอะไร**, และ **ต้องพิสูจน์อะไรได้จริง**  
> รายละเอียดธุรกิจให้อ้างอิง `BMC.md` และรายละเอียดสถานะงานให้อ้างอิง `docs/BANGSAEN_WASTE_VISION_PLAN.md`

## 1. ชื่อและเป้าหมาย

**ชื่อเว็บ:** Bangsaen Waste Vision

**ปัญหาที่ต้องการช่วยแก้:**  
พื้นที่สาธารณะและทางน้ำบางจุดอาจมีขยะสะสม แต่เจ้าหน้าที่ไม่สามารถเฝ้าดูทุกจุดหรือทุกกล้องได้ตลอดเวลา ระบบจึงช่วยคัดกรองจุดที่ควรตรวจสอบ และให้เจ้าหน้าที่เป็นผู้ยืนยันก่อนดำเนินการ

**ผู้ใช้หลัก**
- เจ้าหน้าที่เทศบาล / เจ้าหน้าที่สิ่งแวดล้อม
- ทีมดูแลความสะอาด ชายหาด และพื้นที่สาธารณะ

**ผู้ใช้รอง**
- ประชาชน ร้านค้า และนักท่องเที่ยวที่ใช้หน้าแจ้งจุดขยะ

---

## 2. ฟีเจอร์หลักที่ต้องมี

### 2.1 หน้าสถิติขยะ
ใช้ข้อมูลเปิดของจังหวัดชลบุรีที่นำเข้าไว้ในฐานข้อมูล

ต้องแสดงอย่างน้อย:
- ปริมาณขยะที่เกิดขึ้น
- ปริมาณที่เก็บขน
- ปริมาณที่นำไปใช้ประโยชน์
- ปริมาณกำจัดถูกต้อง / ไม่ถูกต้อง
- อัตราการกำจัดถูกต้อง
- 5 อปท. ที่มีปริมาณขยะเกิดขึ้นสูงสุด

ข้อมูลส่วนนี้เป็น **ข้อมูลย้อนหลัง** ไม่ใช่ข้อมูลสดจากกล้อง และไม่ใช้เป็นข้อมูลฝึก Computer Vision

### 2.2 หน้าแจ้งจุดขยะ
ผู้ใช้กรอก:
- สถานที่
- ประเภทขยะ
- ปริมาณโดยประมาณ
- รายละเอียดเพิ่มเติม

ระบบต้อง:
- ตรวจข้อมูลฝั่ง server
- บันทึกลง MySQL
- แสดงรายการแจ้งล่าสุดและสถานะ

### 2.3 Waste Vision
เป็นฟีเจอร์เด่นของกลุ่ม

Prototype วันนี้ใช้ **REPLAY MODE** เพราะยังไม่ได้เชื่อม CCTV เทศบาลจริง

แนวคิดหลัก:

```text
ภาพ / วิดีโอทดสอบ
→ Local Computer Vision
→ เก็บผลการตรวจพบแต่ละครั้ง
→ รวมผลที่เกิดซ้ำจากจุดเดียวกันเป็นเหตุเดียว
→ แสดงคิวเหตุที่ควรตรวจ
→ เจ้าหน้าที่ Confirm / Reject
→ ถ้า Confirm = Needs Check
→ เมื่อดำเนินการแล้ว = Resolved
```

หลักสำคัญ:

**การตรวจพบ 1 ครั้ง ไม่เท่ากับ 1 เหตุ และไม่เท่ากับ 1 งาน**

ระบบต้องไม่สร้างงานใหม่ทุกครั้งที่กล้องเห็นขยะซ้ำ

---

## 3. ข้อมูลที่ใช้

### ข้อมูลสถิติ
ไฟล์:
`data/waste_chonburi.csv`

ที่มา:
ข้อมูลเปิดของสำนักงานทรัพยากรธรรมชาติและสิ่งแวดล้อมจังหวัดชลบุรี  
https://chonburi.gdcatalog.go.th/dataset/?organization=chonburi_mnre

ข้อมูลถูกนำเข้า MySQL เพื่อให้เว็บอ่านใช้งาน ไม่ได้ดึงสดจากเว็บไซต์ต้นทางทุกครั้ง

### ข้อมูลแจ้งขยะ
มาจากฟอร์มบนเว็บและบันทึกลง MySQL

### ข้อมูล Waste Vision
มาจากผล inference ของ Local Computer Vision

Prototype ใช้ภาพอ้างอิงสำหรับทดสอบระบบ  
**ยังไม่ใช่ภาพจาก CCTV เทศบาลหรือชุดข้อมูลบางแสนจริง**

---

## 4. ฐานข้อมูล

ตารางหลัก:

- `waste_stats` — ข้อมูลสถิติขยะ
- `reports` — รายการแจ้งจุดขยะ
- `vision_observations` — ผลที่ AI ตรวจพบแต่ละครั้ง
- `vision_incidents` — เหตุที่รวมผลตรวจซ้ำแล้ว เพื่อให้เจ้าหน้าที่ตรวจ

หลักการ:
- คนตรวจและเปลี่ยนสถานะที่ระดับ `vision_incidents`
- `vision_observations` ใช้เป็นข้อมูลประกอบของเหตุ
- confidence เป็นข้อมูลช่วยดูเท่านั้น ไม่ใช่หลักฐานว่าขยะได้รับการยืนยันแล้ว

รายละเอียดคอลัมน์ให้อ้างอิง:
`afternoon/sql/schema.sql`

---

## 5. API หลัก

### สถิติ
- `GET /api/stats.php`

### แจ้งจุดขยะ
- `GET /api/reports.php`
- `POST /api/reports.php`

### Waste Vision
- `GET /api/vision.php` — อ่านรายการเหตุและสรุปสถานะ
- `POST /api/vision.php` — รับผลตรวจจาก Local Computer Vision
- `GET /api/vision-observations.php?incident_id=...` — ดูผลตรวจที่อยู่ในเหตุหนึ่ง
- `POST /api/vision-review.php` — Confirm / Reject / Resolve เหตุ

---

## 6. กฎสำคัญของ Waste Vision

สำหรับ Prototype:
- ผลตรวจที่มาจากจุดเดียวกันและเกิดใกล้กัน สามารถรวมเป็นเหตุเดียวได้
- ค่าช่วงเวลารวมเหตุอยู่ใน `afternoon/lib/vision_config.php`
- กฎนี้ยังเป็น **PROTOTYPE / UNCALIBRATED**
- AI ห้ามยืนยันเหตุแทนคน
- AI ห้ามสร้างคำสั่งเก็บขยะอัตโนมัติ
- เจ้าหน้าที่ต้องเป็นผู้ Confirm หรือ Reject

---

## 7. สิ่งที่ต้อง Demo ได้

ต้องพิสูจน์ได้ว่า:

1. หน้าสถิติอ่านข้อมูลจาก MySQL ได้จริง
2. หน้าแจ้งขยะส่งข้อมูลเข้า MySQL ได้จริง
3. Local Computer Vision รันกับภาพจริงได้
4. ผลตรวจส่งเข้า PHP + MySQL ได้
5. การตรวจซ้ำจากจุดเดียวกันสามารถรวมเป็นเหตุเดียวได้
6. Dashboard แสดงเหตุที่รอตรวจได้
7. เจ้าหน้าที่ Confirm / Reject ได้
8. เหตุที่ Confirm แล้วสามารถเปลี่ยนเป็น Resolved ได้
9. หน้าเว็บระบุ **REPLAY MODE** และ **PROTOTYPE / UNCALIBRATED** ชัดเจน

---

## 8. สิ่งที่ห้ามพูดเกินหลักฐาน

ห้ามอ้างว่า:
- เชื่อม CCTV เทศบาลจริงแล้ว
- โมเดลมีความแม่นยำกับบางแสนตามตัวเลขใด ๆ โดยยังไม่ได้ทดสอบ
- ตรวจขยะได้ทุกกล้องหรือทุกสภาพ
- กฎรวมเหตุผ่านการปรับเทียบกับพื้นที่จริงแล้ว
- AI เป็นผู้ยืนยันเหตุหรือสั่งงานแทนเจ้าหน้าที่
- ระบบลดเวลา ค่าใช้จ่าย หรือแรงงานได้กี่เปอร์เซ็นต์โดยยังไม่ได้ทดลองจริง

---

## 9. Definition of Done สำหรับ Workshop

ถือว่าผ่านเมื่อ:
- Python tests ผ่าน
- PHP validation tests ผ่าน
- Stats ใช้ข้อมูลจริงจากฐานข้อมูล
- Report บันทึกและอ่านข้อมูลได้
- Waste Vision รัน detector จริงอย่างน้อย 1 ภาพ
- ผล detector เข้า MySQL ได้
- ระบบรวมการตรวจซ้ำเป็น Incident ได้
- Human Review ทำงานที่ระดับ Incident
- Demo ทำงานผ่าน localhost
- ไม่มีข้อความหรือ claim เกินสิ่งที่พิสูจน์แล้ว
