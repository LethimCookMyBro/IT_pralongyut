# secrets
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/

# local runtime output (detector worker writes here)
afternoon/runtime/
runtime/

# logs / temp
*.log
*.tmp
*.temp
.DS_Store
Thumbs.db

# IDE / user-local
# .vscode/settings.json is a workshop file and stays tracked on purpose.
.idea/
*.code-workspace

# third-party detector checkout + model weights (107MB, license not verified).
# Kept locally, never published. See RAILWAY_DEPLOY.md / references note.
references/pLitter/
*.pt

# Phase D local video screening outputs / large reference clips
Video/
references/spike/candidate_eval/
references/spike/detect-*.png
references/test_videos/*.mp4
references/test_videos/*.mov
references/test_videos/*.avi
references/test_videos/*.mkv
references/test_videos/*.webm

# รูปที่ประชาชนอัปโหลดผ่านหน้าแจ้งจุดขยะ — เป็น user content ไม่ commit
# (โฟลเดอร์ยังถูกเก็บไว้ด้วย .gitkeep เพื่อให้ deploy แล้วมีที่เขียน)
afternoon/uploads/reports/*
!afternoon/uploads/reports/.gitkeep
!afternoon/uploads/reports/.htaccess

# วิดีโอ demo ที่ render จาก inference จริง — ใหญ่และ derive ได้จาก local_vision/render_demo_videos.py
afternoon/runtime/demo-videos/
